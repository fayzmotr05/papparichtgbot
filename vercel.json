{
  "buildCommand": "cd mini-app && npm install && npm run build",
  "outputDirectory": "mini-app/dist",
  "installCommand": "cd mini-app && npm install",
  "devCommand": "cd mini-app && npm run dev",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}